# Copy to .env and fill in real values.
# Anthropic Claude is used for generation (LLM).
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI is used for embeddings only (LlamaIndex has no native Anthropic embeddings).
OPENAI_API_KEY=sk-...
